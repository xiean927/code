* 我在Dijkstra内计算最短距离,和示例代码不同

```
#include <bits/stdc++.h>

using namespace std;

const int maxn=1050;///起始写1050也是可以的
int n,m,k,MaxDis;
int e[maxn][maxn];
int d[maxn];
double avg[maxn];///计算平均距离
int pnum[maxn];///记录该道路上一共有多少结点

bool vis[maxn];



void Dijkstra(int st){
    d[st]=0;
    pnum[st]=1;
    for(int i=1;i<=n;i++){
        int u=-1,minn=inf;
        for(int j=1;j<=n;j++){
            if(minn>d[j]&&vis[j]==false){
                u=j;
                minn=d[j];
            }
        }
        if(u==-1)
            break;
        vis[u]=true;
        for(int v=1;v<=n;v++){
            if(vis[v]==false&&e[u][v]!=inf){
                if(d[v]>d[u]+e[u][v]){
                    d[v]=d[u]+e[u][v];
                    pnum[v]=pnum[u]+1;
                }
                else if(d[v]==d[u]+e[u][v]){
                    if()
                }
            }
        }
    }
}


int main(){
    scanf("%d%d%d%d",&n,&m,&k,&MaxDis);
    fill(e[0],e[0]+maxn*maxn,inf);
    string a,b;
    int dis;
    int ia,ib;
    for(int i=0;i<k;i++){
        cin>>a>>b>>dis;
        ia=0,ib=0;
        if(a[0]=='G'){
            for(int j=1;j<(int)a.size();j++)
                ia=ia*10+(a[j]-'0');
            ia+=n;
        }
        else{
            for(int j=0;j<(int)a.size();j++)
                ia=ia*10+(a[j]-'0');
        }

        if(b[0]=='G'){
            for(int j=1;j<(int)b.size();j++)
                ib=ib*10+(b[j]-'0');
            ib+=n;
        }
        else{
            for(int j=0;j<(int)b.size();j++)
                ib=ib*10+(b[j]-'0');
        }
        e[ia][ib]=e[ib][ia]=dis;
        //cout<<ia<<" "<<ib<<endl;
    }
    for(int i=n+1;i<=n+m;i++){
        fill(d,d+maxn,inf);
        fill(vis,vis+maxn,false);
        memset(avg,0,sizeof(avg));
        memset(pnum,0,sizeof(pnum));
    }

    return 0;
}


```
